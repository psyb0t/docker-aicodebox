# Secrets — never commit
.env
.env.*
*.env
!.env.example

# Local tooling / scratch
AGENTS.md
CODEX.local.md
ANUSTIMES.md
CLAUDE.md
git-update.sh
pyrightconfig.json
.mypy_cache/
.pytest_cache/
.ruff_cache/
.testing/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# Local venvs
.venv/
venv/
.telemetry

# Full-image package-manager cache. The pinned manifests under full-node/ are
# committed, but pnpm's local store is build output.
full-node/.pnpm-store/

# Cloned upstream sources read during research — scratch, never shipped
.research_files/
